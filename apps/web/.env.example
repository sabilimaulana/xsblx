# The API origin is normally injected by the stack (`VITE_API_URL: api.url`), so
# this file only matters when running `vite dev` on its own, outside
# `alchemy dev`.
VITE_API_URL=http://localhost:3000
